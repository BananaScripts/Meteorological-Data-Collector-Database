{
  "devDependencies": {
    "@prisma/client": "^5.20.0",
    "prisma": "^5.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  },
  "scripts": {
    "migrate": "npx prisma migrate dev",
    "generate": "npx prisma generate",
    "start": "npm run migrate && npm run generate && node database.ts && npx prisma migrate deploy"
  }
}
